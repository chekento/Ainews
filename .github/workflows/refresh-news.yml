name: Refresh AI news

on:
  workflow_dispatch:
  schedule:
    - cron: '7,37 * * * *'
  push:
    branches: [main]
    paths:
      - 'app.js'
      - 'index.html'
      - 'styles.css'
      - 'sw.js'
      - 'scripts/**'
      - 'config/**'
      - '.github/workflows/refresh-news.yml'

permissions:
  contents: write

concurrency:
  group: ai-news-refresh
  cancel-in-progress: true

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Validate JavaScript and JSON
        run: |
          node --check app.js
          node --check scripts/fetch-news.mjs
          node -e "JSON.parse(require('fs').readFileSync('config/sources.json','utf8')); JSON.parse(require('fs').readFileSync('config/providers.json','utf8')); console.log('JSON OK')"
      - name: Build AI-only news dataset
        run: node scripts/fetch-news.mjs
      - name: Commit updated dataset
        run: |
          if git diff --quiet -- data/news.json; then
            echo "No dataset change."
            exit 0
          fi
          git config user.name "ai-news-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/news.json
          git commit -m "data: refresh AI news feed"
          git pull --rebase origin main
          git push origin HEAD:main
